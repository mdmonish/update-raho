import React from 'react';

const LineChart = ({coinHistory,currentPrice,coinName}) => {
    // console.log("line",coinHistory,currentPrice,coinName)
  return (
    <div>
      Line
    </div>
  );
}

export default LineChart;
